import { Context } from "../../utils"

export const addMembers = {
  //   async addMembers(parent, args, ctx: Context, info) {
  //     //   return ctx.db.mutation.updateGroup(
  //     //       { where: {id: args.group},
  //     //     data: {members: {push: args.users}}}
  //     //   )
  //   }
}
